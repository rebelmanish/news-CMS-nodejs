<% if(errors.length > 0 ){ %>
    <div class="errors alert alert-danger mt-2">
        <ul>
            <% errors.forEach(function(error){ %>
                <li><%= error.msg %></li>
            <% }) %>
        </ul>
     </div>
<% } %>